Additions:
Added tab completions for /clan setcolor, /clan accept, /clan decline, /clan autodecline, /clan lock, /clan lockchunk, /clan unlockchunk, /clan grantaccess, /clan denyaccess, /opclan claim, /opclan tp, /opclan setcolor, /opclan setdescription, /opclan setname, /opclan set

Changes:
Updated the help description for /clan invite
Improved tab completions for /opclan kick, /opclan setserver

Fixes/Internal Changes:
Fixed the permission command.clans.clan.claim.radius not being registered with Sponge
Fixed NullPointerException printing to console when attempting a tab completion for an invalid subcommand
Fixed tab completions not showing when using an alias of a subcommand
Fixed tab completions for /opclan autoclaim, /opclan setrank
Fixed clan name cache being empty in some cases where it shouldn't
Fixed buckets being usable on foreign territory
