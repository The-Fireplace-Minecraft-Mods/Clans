Additions:
Added a config option to allow disabling the clan home acting as a fallback spawnpoint for players without a bed

Changes:

Fixes/Internal Changes:
Fix the error when attempting to access a locked block which doesn't have any overrides set
